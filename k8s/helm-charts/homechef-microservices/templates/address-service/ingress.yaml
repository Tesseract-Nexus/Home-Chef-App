{{- if and .Values.addressService.enabled .Values.addressService.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "homechef.fullname" . }}-address-service
  labels:
    {{- include "homechef.labels" . | nindent 4 }}
    app.kubernetes.io/component: address-service
spec:
  {{- if .Values.addressService.ingress.className }}
  ingressClassName: {{ .Values.addressService.ingress.className }}
  {{- end }}
  {{- if .Values.addressService.ingress.tls }}
  tls:
    {{- range .Values.addressService.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.addressService.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "homechef.fullname" $ }}-address-service
                port:
                  number: {{ $.Values.addressService.service.port }}
          {{- end }}
    {{- end }}
{{- end }}