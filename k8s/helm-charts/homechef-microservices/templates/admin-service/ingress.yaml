{{- if and .Values.adminService.enabled .Values.adminService.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "homechef.fullname" . }}-admin-service
  labels:
    {{- include "homechef.labels" . | nindent 4 }}
    app.kubernetes.io/component: admin-service
  annotations:
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
    nginx.ingress.kubernetes.io/websocket-services: {{ include "homechef.fullname" . }}-admin-service
spec:
  {{- if .Values.adminService.ingress.className }}
  ingressClassName: {{ .Values.adminService.ingress.className }}
  {{- end }}
  {{- if .Values.adminService.ingress.tls }}
  tls:
    {{- range .Values.adminService.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.adminService.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "homechef.fullname" $ }}-admin-service
                port:
                  number: {{ $.Values.adminService.service.port }}
          {{- end }}
    {{- end }}
{{- end }}