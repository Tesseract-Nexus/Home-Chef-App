{{- if .Values.analyticsService.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "homechef.fullname" . }}-analytics-service-config
  labels:
    {{- include "homechef.labels" . | nindent 4 }}
    app.kubernetes.io/component: analytics-service
data:
  PORT: {{ .Values.analyticsService.env.PORT | quote }}
  HOST: {{ .Values.analyticsService.env.HOST | quote }}
  ENVIRONMENT: {{ .Values.analyticsService.env.ENVIRONMENT | quote }}
  SSL_CERT_PATH: "/root/certs/tls.crt"
  SSL_KEY_PATH: "/root/certs/tls.key"
{{- end }}