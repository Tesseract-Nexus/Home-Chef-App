{{- if and .Values.analyticsService.enabled .Values.analyticsService.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "homechef.fullname" . }}-analytics-service
  labels:
    {{- include "homechef.labels" . | nindent 4 }}
    app.kubernetes.io/component: analytics-service
spec:
  {{- if .Values.analyticsService.ingress.className }}
  ingressClassName: {{ .Values.analyticsService.ingress.className }}
  {{- end }}
  {{- if .Values.analyticsService.ingress.tls }}
  tls:
    {{- range .Values.analyticsService.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.analyticsService.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "homechef.fullname" $ }}-analytics-service
                port:
                  number: {{ $.Values.analyticsService.service.port }}
          {{- end }}
    {{- end }}
{{- end }}