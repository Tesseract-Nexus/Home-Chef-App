{{- if and .Values.chefService.enabled .Values.chefService.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "homechef.fullname" . }}-chef-service
  labels:
    {{- include "homechef.labels" . | nindent 4 }}
    app.kubernetes.io/component: chef-service
  {{- with .Values.chefService.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.chefService.ingress.className }}
  ingressClassName: {{ .Values.chefService.ingress.className }}
  {{- end }}
  {{- if .Values.chefService.ingress.tls }}
  tls:
    {{- range .Values.chefService.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.chefService.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "homechef.fullname" $ }}-chef-service
                port:
                  number: {{ $.Values.chefService.service.port }}
          {{- end }}
    {{- end }}
{{- end }}