{{- if and .Values.customerService.enabled .Values.customerService.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "homechef.fullname" . }}-customer-service
  labels:
    {{- include "homechef.labels" . | nindent 4 }}
    app.kubernetes.io/component: customer-service
spec:
  {{- if .Values.customerService.ingress.className }}
  ingressClassName: {{ .Values.customerService.ingress.className }}
  {{- end }}
  {{- if .Values.customerService.ingress.tls }}
  tls:
    {{- range .Values.customerService.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.customerService.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "homechef.fullname" $ }}-customer-service
                port:
                  number: {{ $.Values.customerService.service.port }}
          {{- end }}
    {{- end }}
{{- end }}