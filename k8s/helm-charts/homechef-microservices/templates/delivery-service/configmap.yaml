{{- if .Values.deliveryService.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "homechef.fullname" . }}-delivery-service-config
  labels:
    {{- include "homechef.labels" . | nindent 4 }}
    app.kubernetes.io/component: delivery-service
data:
  PORT: {{ .Values.deliveryService.env.PORT | quote }}
  HOST: {{ .Values.deliveryService.env.HOST | quote }}
  ENVIRONMENT: {{ .Values.deliveryService.env.ENVIRONMENT | quote }}
  SSL_CERT_PATH: "/root/certs/tls.crt"
  SSL_KEY_PATH: "/root/certs/tls.key"
{{- end }}