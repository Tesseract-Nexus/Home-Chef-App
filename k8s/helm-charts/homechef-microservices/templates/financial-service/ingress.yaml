{{- if and .Values.financialService.enabled .Values.financialService.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "homechef.fullname" . }}-financial-service
  labels:
    {{- include "homechef.labels" . | nindent 4 }}
    app.kubernetes.io/component: financial-service
spec:
  {{- if .Values.financialService.ingress.className }}
  ingressClassName: {{ .Values.financialService.ingress.className }}
  {{- end }}
  {{- if .Values.financialService.ingress.tls }}
  tls:
    {{- range .Values.financialService.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.financialService.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "homechef.fullname" $ }}-financial-service
                port:
                  number: {{ $.Values.financialService.service.port }}
          {{- end }}
    {{- end }}
{{- end }}