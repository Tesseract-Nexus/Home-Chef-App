{{- if .Values.financialService.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "homechef.fullname" . }}-financial-service
  labels:
    {{- include "homechef.labels" . | nindent 4 }}
    app.kubernetes.io/component: financial-service
spec:
  type: {{ .Values.financialService.service.type }}
  ports:
    - port: {{ .Values.financialService.service.port }}
      targetPort: {{ .Values.financialService.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "homechef.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: financial-service
{{- end }}