{{- if and .Values.locationService.enabled .Values.locationService.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "homechef.fullname" . }}-location-service
  labels:
    {{- include "homechef.labels" . | nindent 4 }}
    app.kubernetes.io/component: location-service
spec:
  {{- if .Values.locationService.ingress.className }}
  ingressClassName: {{ .Values.locationService.ingress.className }}
  {{- end }}
  {{- if .Values.locationService.ingress.tls }}
  tls:
    {{- range .Values.locationService.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.locationService.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "homechef.fullname" $ }}-location-service
                port:
                  number: {{ $.Values.locationService.service.port }}
          {{- end }}
    {{- end }}
{{- end }}