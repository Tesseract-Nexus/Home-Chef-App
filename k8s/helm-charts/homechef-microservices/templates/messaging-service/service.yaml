{{- if .Values.messagingService.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "homechef.fullname" . }}-messaging-service
  labels:
    {{- include "homechef.labels" . | nindent 4 }}
    app.kubernetes.io/component: messaging-service
spec:
  type: {{ .Values.messagingService.service.type }}
  ports:
    - port: {{ .Values.messagingService.service.port }}
      targetPort: {{ .Values.messagingService.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "homechef.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: messaging-service
{{- end }}