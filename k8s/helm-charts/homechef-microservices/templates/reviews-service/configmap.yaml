{{- if .Values.reviewsService.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "homechef.fullname" . }}-reviews-service-config
  labels:
    {{- include "homechef.labels" . | nindent 4 }}
    app.kubernetes.io/component: reviews-service
data:
  PORT: {{ .Values.reviewsService.env.PORT | quote }}
  HOST: {{ .Values.reviewsService.env.HOST | quote }}
  ENVIRONMENT: {{ .Values.reviewsService.env.ENVIRONMENT | quote }}
  SSL_CERT_PATH: "/root/certs/tls.crt"
  SSL_KEY_PATH: "/root/certs/tls.key"
{{- end }}