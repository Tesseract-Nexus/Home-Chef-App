{{- if and .Values.reviewsService.enabled .Values.reviewsService.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "homechef.fullname" . }}-reviews-service
  labels:
    {{- include "homechef.labels" . | nindent 4 }}
    app.kubernetes.io/component: reviews-service
spec:
  {{- if .Values.reviewsService.ingress.className }}
  ingressClassName: {{ .Values.reviewsService.ingress.className }}
  {{- end }}
  {{- if .Values.reviewsService.ingress.tls }}
  tls:
    {{- range .Values.reviewsService.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.reviewsService.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "homechef.fullname" $ }}-reviews-service
                port:
                  number: {{ $.Values.reviewsService.service.port }}
          {{- end }}
    {{- end }}
{{- end }}