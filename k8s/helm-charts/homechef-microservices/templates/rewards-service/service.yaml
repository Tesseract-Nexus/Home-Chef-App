{{- if .Values.rewardsService.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "homechef.fullname" . }}-rewards-service
  labels:
    {{- include "homechef.labels" . | nindent 4 }}
    app.kubernetes.io/component: rewards-service
spec:
  type: {{ .Values.rewardsService.service.type }}
  ports:
    - port: {{ .Values.rewardsService.service.port }}
      targetPort: {{ .Values.rewardsService.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "homechef.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: rewards-service
{{- end }}