{{- if and .Values.searchService.enabled .Values.searchService.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "homechef.fullname" . }}-search-service
  labels:
    {{- include "homechef.labels" . | nindent 4 }}
    app.kubernetes.io/component: search-service
spec:
  {{- if .Values.searchService.ingress.className }}
  ingressClassName: {{ .Values.searchService.ingress.className }}
  {{- end }}
  {{- if .Values.searchService.ingress.tls }}
  tls:
    {{- range .Values.searchService.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.searchService.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "homechef.fullname" $ }}-search-service
                port:
                  number: {{ $.Values.searchService.service.port }}
          {{- end }}
    {{- end }}
{{- end }}